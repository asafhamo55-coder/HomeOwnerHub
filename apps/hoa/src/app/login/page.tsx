// Placeholder so middleware has a real /login destination during scaffolding.
// Replaced by the magic-link form in the next checkpoint.
export default function LoginPage() {
  return (
    <main className="grid min-h-screen place-items-center p-8">
      <div className="w-full max-w-sm space-y-2 text-center">
        <h1 className="text-2xl font-bold text-muted">HOA Hub</h1>
        <p className="text-sm text-muted-fg">Sign-in form coming in the next checkpoint.</p>
      </div>
    </main>
  )
}
